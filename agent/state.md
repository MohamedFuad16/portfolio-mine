# State

> Last updated: 2026-07-29 · HEAD: 3206752 (main, pushed and deployed)

## Current state summary

The workspace contains a Vite React portfolio inspired by `manixh.dev`, populated with Mohamed Fuad's CV content. It uses a professional headshot with a two-sided LinkedIn QR card, bilingual EN/JA copy, expandable work rows, a live rolling-12-month GitHub contribution grid, GSAP motion, and hash-routed project detail views. The homepage positions Mohamed as a third-year Information and Communication Technology student working toward Forward Deployed Engineering, with the symbol-only Tokai University mark in the profile metadata. Project cards use consistent 16:9 previews and a full-width, single-line technology rail. ClaudeShot has replaced Codex Account Switcher. On mobile, the entire tapped project card expands into the full-screen detail overlay and reverses back to its original rectangle on close. Latest checks (2026-07-22) verified the transition in flight and at both endpoints, reverse navigation, desktop/mobile layout, the symbol-only university mark, no horizontal overflow, and a clean production build. Install and build with pnpm.

## Recent changes

- 2026-07-29 (mobile expand glitch found by recording it, ADR-046): The user reported the mobile project-open still "realigns a bit". Recorded it properly for the first time — headless Chrome at 390x844 with touch emulation, a real dispatched tap, `Page.startScreencast`, and a per-`rAF` geometry sampler — because the preview pane freezes `rAF` and every earlier verification of this animation (ADR-029/036/038/040) had been done with synthetic clicks against a frozen timeline. Cause: `.pd-expand-face .project { width: 100% }` sizes the cloned card as a percentage of the overlay, and the overlay's width is exactly what the open tweens, so the clone re-laid out every frame — its grid column went 306→338→350px, its preview image drifted 33.6px wider, and **its title slid 171.9px down the screen**. Fixed with `pinnedFaceLayout(origin)` on the clone in both the open and close timelines, the same treatment ADR-036 gave `.project-detail-inner`. Note GSAP defaults `minHeight` to percent, not px — the first attempt wrote `min-height: 404.75%` and made the clone 3121px tall, so the helper returns explicit px strings. After the fix every clone child matches its original exactly and drift is 0.0px in width, height and title position across all three cards tested, open and close. Production build clean.

- 2026-07-29 (JA job title localized; order changed again): "Forward Deployed Engineer" was still Latin script inside the Japanese intro — now フォワードデプロイドエンジニア. Swept the whole `copy.ja` block for other untranslated runs of English and found none; the remaining Latin strings there are product names (MCP, TypeScript, Swift, AWS…) which correctly stay as they are. The English copy keeps the English title. Card order is now WebDrop, Internship Portal, AI Brain Platform, Tutor-System, TokaiHub, ClaudeShot.

- 2026-07-29 (AI Brain public dashboard; New flag removed): The AI Brain Platform's generated gym dashboard is now published at `https://brain.mohamedfuad.com` — verified it serves the same "AI Brain · Gym" page the card's preview image was captured from — so the card and detail page carry a Live link alongside the existing "Private repo" label (the repo itself is still private). Removed the "New" flag added earlier the same day at the user's request: the `isNew` field, the `newBadge` copy in both locales, the `.project-new` rule, and the render branch are all gone; `.project-badge` stays as the explicit class it was given. Order and the full-width title row are unchanged. Production build clean.

- 2026-07-29 (project order, New flag, title row — ADR-045): Reordered the cards to AI Brain Platform, WebDrop, Internship Portal, Tutor-System, TokaiHub, ClaudeShot. Gave the Internship Portal a green "New" flag at the top-left of its preview, driven by a `project.isNew` flag rather than a hardcoded slug; top-left is the only free corner since the status badge is bottom-right and the View-details hint is bottom-left and permanently visible on mobile. Moved the Live/GitHub actions onto their own line beneath the title: ADR-032 had pinned them to one row, which was clipping three of six titles by ellipsis on the live site ("Internship Portal" needed 137px and got 96, "AI Brain Platform" 141/100, and "Tutor-System" 114/96 — a latent bug the two longer new titles exposed). All six titles now measure `scrollWidth === clientWidth` at 375px and 1280px in both locales, with no overlap between the new flag and the existing overlays. Production build clean.

- 2026-07-29 (flow-chart drawing quality, ADR-044): The user asked why the system-architecture diagram did not look clean — "not the straight lines alone, but the drawing itself." Captured the chart at 3.2x over CDP with the `#fc-sketch` hand-drawn filter on and forced off, and diffed: **pixel-identical, max channel delta 0.** The filter chain worked (a blur primitive blurred everything; `scale=18` crinkled it) but at `baseFrequency 0.02` / `scale 2.4` it displaced by at most ~1 user unit and rounded away entirely. The chart was therefore machine-perfect geometry wearing a handwriting font, which is exactly what read as "not clean", while costing 13 chart-sized filter buffers per paint. Removed the filter and committed to a precise diagram: rounded joins and caps, non-scaling strokes, connectors lightened to 1.5 against the shapes' 2 so the boxes lead, arrowheads matched to their line, and subtitles mixed 45% toward the page text colour because the raw Excalidraw tone was barely legible on near-black. Also removed the "By the numbers" strip from the Internship Portal page at the user's request (kept on AI Brain), re-shot the AI Brain preview at a 2048x1152 logical viewport so the frame is filled with dashboard rather than empty space, and trimmed two Japanese branch labels that had no margin in the 116-unit phone box. Verified all six charts in EN and JA at 375px and 1280px; production build clean.

- 2026-07-29 (two current projects added, ADR-043): Researched the user's two active repos and added them as the first two project cards. **Internship Portal** (`resume-studio-dashboard`, public, live) — a bilingual internship tracker, résumé editor and LaTeX-to-PDF compiler shipping a React web app and a SwiftUI iOS app from one repository, whose defining rule is that user data never touches the server (clients read/write Firestore directly under owner-only rules; the Express server on Azure owns only the shared catalog, the compile endpoint and a Gmail action queue). **AI Brain Platform** (`ai-brain-platform`, private) — the user's own research into making a cheap model behave like a long-time teammate without any fine-tuning: versioned Markdown as the source of truth, Postgres+pgvector as a fully rebuildable index, RAG plus an MCP server for retrieval, and a graded exam loop that replays real git history. Screenshots were captured from the live site and from the repo's own committed dashboard at 1440x810 over the DevTools protocol. `project.github` is now optional so the private repo shows a "Private repo" label instead of a dead link, and detail pages gained optional `highlights` (a four-figure strip) and `status` (where the project honestly stands) blocks. Also fixed TokaiHub's live URL, which still pointed at GitHub Pages. Verified in EN and JA at 375px and 1280px; production build clean.

- 2026-07-29 (repo-wide bug sweep, ADR-042): Pulled four automated snapshot commits, then audited the whole repo and fixed everything found. Layout: timeline dots were 12px off the dashed line for the entire 561-760px band (the `<=760px` `.dot` offset assumed a negative summary margin that does not apply until `<=560px`), and at `<=470px` the project title and Live/GitHub buttons were centred over a left-aligned card because the stacked heading inherited `align-items: center`. The contribution grid was rebuilt from a 35-column row-major block of 245 days into 52 week-columns x 7 rows covering a real 12 months, with month labels anchored to the week each month starts in — previously the nine evenly-spaced labels sat above days they had nothing to do with, and the caption claimed 12 months over ~8 months of data. Behaviour: "Back to projects" used `history.length > 1` and could send a visitor who arrived from a shared link straight back off the site; the detail overlay declared `role="dialog" aria-modal="true"` while Escape did nothing, focus never entered it, and 33 background controls stayed tabbable. Both fixed, with focus deferred until the entrance tween actually makes the overlay visible. Toolchain: added `.nvmrc` + `engines` (Node 20.11 on this machine fails every Vite 8 command), added the missing `vite.config.js` so the dev server finally has Fast Refresh, guarded `createRoot` against HMR re-entry, moved build deps to `devDependencies`, dropped the unused `qrcode`, and marked the package private. Content and robustness: the Japanese CV was a byte-identical copy of the English one and has been replaced with the real 履歴書・職務経歴書 supplied by the user, restoring the locale-aware résumé link, the stale hardcoded `561` fallback total is now derived from the data, an error boundary plus a date-shape filter keep one malformed API row from blanking the page, Japanese-locale screen-reader strings are localized, CDN icon URLs are pinned with an `onError` fallback, and 16 unused images (~916 KB) were deleted. README drift corrected along the way (React 18 -> 19, Codex Account Switcher -> ClaudeShot, a `qrcode` dependency the site never used). Verified by measurement at 320/375/400/561/700/760/761/1280px in both locales; production build clean.

- 2026-07-26 (flow chart connector lines invisible, ADR-041): The user reported the system-architecture flow chart's connecting lines were not visible at all. Confirmed via `getBBox()` on the live `.fc-edge` paths: every connector is a straight axis-aligned line, so its bounding box has zero width or zero height — and the shared `#fc-sketch` hand-drawn-wobble filter used the SVG default `objectBoundingBox` units, whose region is a percentage of that box. A percentage of zero is zero, so the filter region collapsed to nothing and every connector line silently failed to render (boxes/diamonds were unaffected since they have real bounding boxes). Fixed by switching `#fc-sketch` to `filterUnits="userSpaceOnUse"` with an explicit pixel region sized to the chart's own SVG dimensions. Verified live at both 375px and desktop widths: every connector line and arrowhead now renders with its label, no console errors, clean production build.

- 2026-07-26 (mobile detail header desync fix, ADR-040): The user reported the mobile project-detail overlay still glitched on open after the ADR-038 origin-measurement fix — the hero image appeared, then the back button and title visibly popped in afterward, reading as an "up and down" settle. Root cause: `.pd-back` and `.pd-headline` carried both `pd-animate` and `pd-reveal`, while `.pd-shot` (the image) only ever carried `pd-animate` — so the header sat on the delayed, staggered `pd-reveal` track (tween starts at t=0.25 in the open timeline) while the image rendered immediately. Fixed by dropping `pd-reveal` from `.pd-back`/`.pd-headline` so all three appear together; `.pd-block` sections (below the fold) intentionally kept their stagger. Verified live at 375px width: `.pd-back`/`.pd-headline`/`.pd-shot` all read `className` without `pd-reveal` and all sit at `opacity:1` together at the same instant, no console errors. Production build clean.

- 2026-07-26 (avatar glow offset, bio split, real mobile card-expand bug, Excalidraw flow chart — ADR-038/039): Fixed a real mobile-only bug the user caught: the border beam's glow was visibly offset from the avatar because `.avatar` had its own hardcoded breakpoint size never mirrored onto `.avatar-shell` (which sizes the beam) — now `.avatar`/`.avatar-beam` always fill `.avatar-shell`, the single sizing authority. Split the bio into two `<p>` (`introParagraphs`, both locales). Found and fixed the actual cause of the still-recurring mobile card-expand glitch: the origin card's rect was captured once at click time and reused later inside a `useGSAP` effect that only fires after an async hash-change round trip — during that gap, GSAP ScrollSmoother's touch-flick momentum can still be moving the list on a real phone (invisible to any synthetic click, which is why it kept passing verification). Fixed by re-measuring the live card fresh at the moment each open/close animation actually builds. Rebuilt `ProjectFlowChart` to match the user's reference design exactly: self-hosted Excalidraw's own "Excalifont" hand-drawn typeface (`public/fonts/Excalifont-Regular.woff2`, MIT/self-hosting-documented package, no new runtime dependency), Excalidraw's default color palette per stage kind via a `--fc-tone` CSS var, more rounded corners, and a shared `#fc-sketch` SVG filter for a hand-drawn wobble on every shape/edge (not text). Verified zero overflow/overlap across all four projects at both flowchart geometries, and the card-expand fix with a real scroll-then-immediately-tap sequence. Production build clean.

- 2026-07-26 (real mobile bugs found + fixed, ADR-037): The previous session's mobile check missed two genuine bugs, caught this session via real scroll/tap testing instead of JS-dispatched scroll. (1) The border beam's blurred glow bled past the avatar's rounded corner (BorderBeam's own overflow:hidden doesn't reliably clip a filtered child in this engine) — fixed with `contain: paint` on `.avatar-beam`. (2) The entire Work Experience list and every contribution-grid cell rendered permanently invisible on load — caused by `ContributionGrid`'s cells sharing `key={day.date}` between fallback and real data, so React reused DOM nodes across the swap and the cell-reveal ScrollTrigger's cached position (from before real data changed the layout) never refreshed; fixed with a `useEffect(() => ScrollTrigger.refresh(), [cells, total])`. Re-verified experience-card tap-to-expand, the mobile project-open animation, and both flow charts on mobile — all clean. Production build passed.

- 2026-07-25 (dependency refresh + eight fixes, ADR-032..036): Updated every dependency to latest (React 19.2.8, Vite 8.1.5, lucide-react 1.26.0, @vitejs/plugin-react 6.0.4) and added `border-beam` + `thinking-orbs`. Work rows now toggle from anywhere on the card via a stretched chevron hit area, with the timeline dots re-anchored so they sit exactly on the dashed line and centre on rows of any height. Contributions are refreshed every 6 hours by a new GitHub Action into `public/assets/contributions.json`, read before the live API (the section still has no title). Project cards keep Live/GitHub on the title line at every width. A border beam traces the profile photo and a thinking orb replaces the sparkle on the "Building AI agent tools" line, both gated on reduced motion. Project detail pages gained a real SVG system-architecture flow chart (terminators, decision diamond, datastore, labelled arrows) beside the existing system map. The mobile card-expand no longer bounces: the inner column is pinned to its final width so the tagline cannot re-wrap mid-animation (measured title shift 18px -> 0px). All external links carry `rel="noopener noreferrer"`. Production build clean.

- 2026-07-22 (silent interactive controls, ADR-031): Limited the achievement sound to clicks on non-interactive page surfaces. Buttons, links, form controls, summaries, labels, ARIA button-like elements, and contenteditable elements no longer trigger audio; the rocket and QR buttons also no longer play sound directly. The ripple remains independent from audio for clicks that bubble to the page handler. Production build passed.

- 2026-07-22 (supplied achievement sound, ADR-030): Replaced the retro coin MP3 with the user's supplied 3.6-second Mixkit "Achievement completed" WAV. The interaction audio now preloads and clones `public/assets/achievement-completed.wav` for page, QR, and rocket clicks; the earlier `retro-coin.mp3` asset was removed. Production build and asset-path checks passed.

- 2026-07-22 (reference-accurate full-card expansion, ADR-029): Superseded the preview-only mobile transition with a clone of the complete tapped project card. The clone expands from the measured card rectangle to the viewport over 440ms with the reference cubic-bezier curve while the page scales to 0.95 and fades to 0.65; closing reverses into the original card over 380ms. Replaced the short tap with a longer retro game coin sound, renamed the asset to `retro-coin.mp3`, and reduced the Tokai University asset to its symbol only. Verified the transition in flight, final fullscreen state, reverse close, reduced-motion fallback, 390px and 1440px layouts, and the production build.

- 2026-07-21 (card layout + ClaudeShot + mobile shared transition, ADR-028): Reworked every project card into a larger 16:9 preview/body row with a full-width technology rail; all five tags fit on one line at desktop and 390px. Reframed Tutor's preview, replaced Codex Account Switcher with ClaudeShot and a dedicated workflow preview, added the official Tokai University wordmark, and changed the global click sound to a short Mixkit coin tap. Mobile project opening now measures the tapped preview and animates the detail image from that exact rectangle into the detail hero over 440ms. EN/JA, desktop/mobile, the Tutor detail route, and the production build were verified.

- 2026-07-21 (portfolio positioning + project-detail refresh, ADR-027): Rewrote the EN/JA hero around the full Information and Communication Technology faculty name and Forward Deployed Engineer goal; labeled Altius Link as formerly KDDI Evolva; changed Thoughts in words to the Qiita profile. Verified the live contribution total at 561, corrected the label to a rolling 12 months, and made month labels data-driven. Recaptured WebDrop, Tutor-System, and TokaiHub in EN/JA at 1440×810, changed previews to fit without cropping, rewrote every detail page from project sources, fixed back-button spacing, and added responsive four-step system maps. Production build and desktop/390px browser verification passed.

- 2026-07-08 (MF logo + Tutor badge): Generated an "MF" monogram favicon — `public/mf-logo.svg` (dark rounded tile, white monoline MF, red accent dot echoing the signature), wired as `<link rel="icon" type="image/svg+xml">` plus a 180px `public/apple-touch-icon.png` fallback in index.html. No on-page logo (user opted to keep the page logo-free). Changed the Tutor-System project badge from "architecture" to "long-term project".

- 2026-07-08 (project detail pages + Qiita + profile pic, ADR-026): Added hash-routed, bilingual project detail pages — clicking a project screenshot opens `#/project/<slug>` as a full-screen overlay (`ProjectDetailView`) that animates in/out with GSAP; content (tagline, overview, key features, how-it-works, tech) lives in each project's `detail: {en,ja}` object, sourced from the GitHub READMEs. `.project-shot` is now a `<button>` with a "View details" hover hint. Changed the "Thoughts in words" link from GitHub to the latest Qiita article (`QIITA_ARTICLE`) with the `SiQiita` logo. Replaced the profile photo with a professional headshot (`public/assets/profile.jpg`, 1000×750, from the yacht photo). Updated README. STILL PENDING: the user wants a new site logo but hasn't provided the file yet.

- 2026-07-06 (tap/locale/icon fixes, ADR-025): (1) Page ripple now listens on `click` instead of `pointerdown`, so scrolling/dragging no longer triggers it — only real taps (skips keyboard clicks via `detail===0`). Verified scroll→0 ripples, click→1. (2) Resume link is locale-aware: `resumeHref` → `/resume/Mohamed_Fuad_CV_JA.pdf` for `ja`, English otherwise (both the profile square and the Let's Connect button). Added `public/resume/Mohamed_Fuad_CV_JA.pdf` as a PLACEHOLDER (byte-copy of the English CV) — must be replaced with the real Japanese resume. (3) Work-experience "done" check shrunk (lucide `Check` size 9→7.5, strokeWidth 3.5→3) for breathing room inside the 16px hollow circle.

- 2026-07-06 (tap effect, ADR-024): Replaced the click ring + 6 flying dots ("wobble") with a water-ripple — three staggered hairline rings expanding (via width/height, not scale) and fading, plus a soft radial center flash. Chime unchanged. `.click-burst` renders 3 `<i>`; removal timeout bumped 900ms→1050ms to outlast the animation. Verified: rings expand 46→88px staggered with graduated opacity, no console errors, clean build.

- 2026-07-06 (mobile polish + signature/perf, ADR-023): Added breathing room between the skills marquees and Work Experience (`.skills-marquees` margin-bottom 1px→26px). Brightened the dashed box borders (#444→#56595d) so project card left/right edges are visible on OLED phones. Tech tags now a `repeat(3, auto)` grid everywhere → always a balanced 3+2 (never orphaned 4+1). "Let's Connect" links become a 2×2 grid at ≤470px (flex row on desktop). Signature lead-in reshaped to a graceful S-swoop into the M (regenerated via scratchpad `build-v3.mjs`). Android-jank fixes: the perpetual signature-trace filter reduced to one small-radius glow, and the trace loop is created paused and only plays while on screen (ScrollTrigger onToggle). Verified on mobile (375) + desktop (1280): tags 3+2, contact 2×2/1-row, no overflow, no console errors, clean build.

- 2026-07-06 (signature tracing loop, ADR-022): Removed the red period-dot; added a perpetual glowing highlight that continuously traces the handwriting. A second overlaid `.sig-trace` path (same data) runs an infinite `yoyo` DrawSVG window (~15% wide) back and forth along the whole stroke while the base `.sig-name` sits dimmed (0.42). Tracer is CSS `opacity:0` by default, enabled only inside the reduced-motion-gated GSAP block. Verified in-browser: highlight travels, no console errors, clean build.
- 2026-07-06 (signature redesign, ADR-021): Replaced the filled Great Vibes signature with a single-stroke "hello"-style monoline of "Mohamed Fuad" — Hershey Script (a rounded connected single-line/centre-line font), rendered as one continuous `stroke` path (no fill) where a flat baseline flows in from the left edge, rises into the name, and flows back out to the right, ending in a red period-dot. Generated offline from the SVG font (scratchpad `build-v3.mjs`). `signature-path.js` now exports `signatureViewBox`/`signaturePath`/`signatureDot`/`signatureStrokeWidth` (no more `signatureBaseline`); stroke width is set via the JS-driven `strokeWidth` attribute (NOT CSS, which would override it). DrawSVG draws the whole path as one gesture, then the dot pops. Verified in-browser: renders, animates, no console errors.
- 2026-07-06 (follow-up, ADR-020): Removed the desktop signature pin — its pinSpacing spacer showed as a big blank gap above the signature; the signature now plays the timed draw on enter everywhere. Moved "Building AI agent tools" to sit directly under the name (identity order: name → building → handle → meta). Verified + clean build.
- 2026-07-06 (GSAP pass 3 + lightbox + responsive fixes, ADR-018/019): Added ScrollSmoother (wrapper/content refs — selectors can't resolve ancestors from the scoped useGSAP context; fixed-position UI lives outside #smooth-content), a DrawSVG "Mohamed Fuad" Great Vibes signature above the wave divider (desktop: pinned scrub draw; mobile: timed draw; generator script in session scratchpad — opentype.js toPathData emits fused numbers that silently truncate the path, so we serialize commands manually), SplitText char reveal on the hero name (reverts on complete), floating skill icons (pipeline reference), continuous equalizer on the wave bars, and an avatar-click lightbox (rotate/scale open, backdrop/Escape close). Fixed the "OR" misalignment root cause (stuck inline transforms from per-child load staggers — hero now animates containers with clearProps), gave the experience hover pill breathing room, restructured ≤560px work rows (date under role, no 82px date column, names wrap), pruned skills to the 15 actually-used techs, and verified everything numerically (reveals, draw completion both breakpoints, lightbox round-trip, equalizer motion, no overflow 375–1280, EN/JA) plus a clean production build.
- 2026-07-06 (reference-image adaptation + GSAP pass 2): Adapted three user-supplied reference screenshots (ADR-016): work-experience timeline nodes are now image-3 style — hollow 16px checked circles for past jobs, solid green node for the active one, still flat/static and centered on the dashed line; the locale button became a segmented EN|日本語 pill (`.locale-switch`, image-1's Talk/Chat toggle); added a `WaveDivider` (dotted baseline + centered waveform bars) between contact card and footer. Upgraded GSAP (ADR-017): coordinated hero entrance timeline, per-card project reveals + tag cascades, scrub-driven parallax inside project screenshots (yPercent ±5 at scale 1.12), timeline node pops (back.out; GSAP preserves the dot's translateY(-50%)), random-order contribution ripple, center-out wave-bar growth, ScrollToPlugin smooth anchor scroll on the hero "#projects" link via contextSafe. Verified: dot/line centers equal on desktop+mobile, all reveals reach opacity 1, parallax provably moves, EN/JA overflow-free, production build clean.
- 2026-07-05 (animation + polish pass): Added GSAP scroll animations. Installed `gsap` + `@gsap/react` **with pnpm** (raw npm crashes on this pnpm-managed tree). All motion lives in one `useGSAP(..., { scope: mainRef })` in `App`, gated behind `gsap.matchMedia('(prefers-reduced-motion: no-preference)')`: hero load stagger, section-title slide-ins, block fade-ups, timeline line-draw + row stagger, project-card stagger, contribution-grid cell ripple, footer fade (uses `start: 'top bottom-=40'` so it can actually fire). Fixed the timeline dots to sit centered on the dashed line (kept the plain flat 10px dot — a fancier glow/pulse version was tried and reverted as bad UX per user). Fixed the "Tutor-System" project title breaking at the hyphen (title `white-space: nowrap`, heading row `flex-wrap: wrap`). Briefly tried a pill-badge treatment for the "Building AI agent tools" link but reverted to the original underlined link per user. See ADR-013..015. Production build clean.
- 2026-07-05: Finished the in-progress JP-locale project screenshots (WebDrop, Tutor-System, TokaiHub) and committed them. Fixed "Hotel SUI Akasaka" (and long company names generally) wrapping mid-word in Work Experience — verified clean in EN and JA at all tested breakpoints (see ADR-010). Replaced the hotlinked JAL favicon with a local copy (ADR-008). Added hover tooltips (date + contribution count, localized, edge-aware positioning) to the contribution heatmap. Thickened the dashed-border style everywhere it's used. Added a `.gitignore` and untracked `node_modules`/`dist`/`.tools` (ADR-009), and deleted the redundant local `.tools/gh` binary from disk (system `gh` already installed via Homebrew). Pushed to `origin/main`.
- 2026-07-05 (same day, follow-up): Full animation/transition audit. Fixed a real jank bug where the heatmap tooltip's fade-out was invisible because `visibility` snapped to hidden before the opacity fade played (ADR-011). Added consistent transitioned hover states to every interactive element site-wide that previously had none or an abrupt one (ADR-012). Re-verified no overflow at 320/375/390/768/1280/1920px in both locales, clean production build. Pushed to `origin/main`.

- 2026-07-04: Created the agent knowledge base and scaffolded the portfolio site from an empty workspace.
- 2026-07-04: Fixed the Vite build script to run `vite build` instead of starting the dev server.
- 2026-07-04: Verified the local page in the in-app browser on desktop and mobile; all images loaded and mobile had no horizontal overflow.
- 2026-07-04: Replaced the generated avatar with the yacht photo, added a logo marquee for skills, strengthened dashed borders, resized the heatmap cells, and swapped project previews to live site screenshots.
- 2026-07-04: Matched more reference measurements: Figtree/JetBrains fonts, 700px outer and 664px inner layout, 120x124 avatar, real LinkedIn QR asset, two opposite-direction skill marquee rows, 1.5px dashed borders, reference-style button shadows, and 300x170 project screenshots.
- 2026-07-04: Added a local transparent avatar decoration layer to mimic the reference profile image treatment, adjusted avatar overflow so the QR hover card is not clipped, rebuilt, and verified no broken images or horizontal overflow at 1280px.
- 2026-07-04: Replaced fragile remote skill/contact icons with `react-icons`, slowed and hover-paused the skill carousel, added an animated/audio rocket name action, official Altius Link and Hotel SUI Akasaka logos, expandable Japanese/English work details, live MohamedFuad16 contribution data, square mobile heatmap scrolling, bottom-right project badges, and a bilingual locale toggle. Rebuilt and verified mobile in the in-app browser with no broken images or horizontal overflow.
- 2026-07-04: Removed the avatar flower decoration, changed the profile QR behavior to a two-sided flip card, moved locale control into the profile handle row with browser-language default and saved preference, added live Tokyo time plus Tokai University status, added page-wide click ring/sound effects, and swapped Hotel SUI Akasaka to the official favicon for a readable work icon. Rebuilt and verified the flip, click effect, mobile heatmap, and no broken images in the in-app browser.
