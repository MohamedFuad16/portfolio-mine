# State

## Current state summary

The workspace contains a Vite React portfolio clone inspired by `manixh.dev`, populated with Mohamed Fuad's CV content. The app builds successfully, runs locally on Vite, uses a yacht profile photo with a two-sided QR flip card, stable local icon components, bilingual English/Japanese copy, expandable work rows with official logos (all local now, including JAL), a live GitHub-style contribution grid with hover tooltips, page-wide click chime/burst effects, and captured project screenshots (including Japanese-locale variants for WebDrop/Tutor-System/TokaiHub). Latest checks (2026-07-05) verified no horizontal overflow at 320/375/768/1280/1920px in both locales, no broken images, no failed network requests, and a clean production build. `node_modules`/`dist` are no longer tracked in git (see ADR-009) — run `npm install` after cloning and `npm run build` before any static-host deploy.

## Recent changes

- 2026-07-06 (signature tracing loop, ADR-022): Removed the red period-dot; added a perpetual glowing highlight that continuously traces the handwriting. A second overlaid `.sig-trace` path (same data) runs an infinite `yoyo` DrawSVG window (~15% wide) back and forth along the whole stroke while the base `.sig-name` sits dimmed (0.42). Tracer is CSS `opacity:0` by default, enabled only inside the reduced-motion-gated GSAP block. Verified in-browser: highlight travels, no console errors, clean build.
- 2026-07-06 (signature redesign, ADR-021): Replaced the filled Great Vibes signature with a single-stroke "hello"-style monoline of "Mohamed Fuad" — Hershey Script (a rounded connected single-line/centre-line font), rendered as one continuous `stroke` path (no fill) where a flat baseline flows in from the left edge, rises into the name, and flows back out to the right, ending in a red period-dot. Generated offline from the SVG font (scratchpad `build-v3.mjs`). `signature-path.js` now exports `signatureViewBox`/`signaturePath`/`signatureDot`/`signatureStrokeWidth` (no more `signatureBaseline`); stroke width is set via the JS-driven `strokeWidth` attribute (NOT CSS, which would override it). DrawSVG draws the whole path as one gesture, then the dot pops. Verified in-browser: renders, animates, no console errors.
- 2026-07-06 (follow-up, ADR-020): Removed the desktop signature pin — its pinSpacing spacer showed as a big blank gap above the signature; the signature now plays the timed draw on enter everywhere. Moved "Building AI agent tools" to sit directly under the name (identity order: name → building → handle → meta). Verified + clean build.
- 2026-07-06 (GSAP pass 3 + lightbox + responsive fixes, ADR-018/019): Added ScrollSmoother (wrapper/content refs — selectors can't resolve ancestors from the scoped useGSAP context; fixed-position UI lives outside #smooth-content), a DrawSVG "Mohamed Fuad" Great Vibes signature above the wave divider (desktop: pinned scrub draw; mobile: timed draw; generator script in session scratchpad — opentype.js toPathData emits fused numbers that silently truncate the path, so we serialize commands manually), SplitText char reveal on the hero name (reverts on complete), floating skill icons (pipeline reference), continuous equalizer on the wave bars, and an avatar-click lightbox (rotate/scale open, backdrop/Escape close). Fixed the "OR" misalignment root cause (stuck inline transforms from per-child load staggers — hero now animates containers with clearProps), gave the experience hover pill breathing room, restructured ≤560px work rows (date under role, no 82px date column, names wrap), pruned skills to the 15 actually-used techs, and verified everything numerically (reveals, draw completion both breakpoints, lightbox round-trip, equalizer motion, no overflow 375–1280, EN/JA) plus a clean production build.
- 2026-07-06 (reference-image adaptation + GSAP pass 2): Adapted three user-supplied reference screenshots (ADR-016): work-experience timeline nodes are now image-3 style — hollow 16px checked circles for past jobs, solid green node for the active one, still flat/static and centered on the dashed line; the locale button became a segmented EN|日本語 pill (`.locale-switch`, image-1's Talk/Chat toggle); added a `WaveDivider` (dotted baseline + centered waveform bars) between contact card and footer. Upgraded GSAP (ADR-017): coordinated hero entrance timeline, per-card project reveals + tag cascades, scrub-driven parallax inside project screenshots (yPercent ±5 at scale 1.12), timeline node pops (back.out; GSAP preserves the dot's translateY(-50%)), random-order contribution ripple, center-out wave-bar growth, ScrollToPlugin smooth anchor scroll on the hero "#projects" link via contextSafe. Verified: dot/line centers equal on desktop+mobile, all reveals reach opacity 1, parallax provably moves, EN/JA overflow-free, production build clean.
- 2026-07-05 (animation + polish pass): Added GSAP scroll animations. Installed `gsap` + `@gsap/react` **with pnpm** (raw npm crashes on this pnpm-managed tree). All motion lives in one `useGSAP(..., { scope: mainRef })` in `App`, gated behind `gsap.matchMedia('(prefers-reduced-motion: no-preference)')`: hero load stagger, section-title slide-ins, block fade-ups, timeline line-draw + row stagger, project-card stagger, contribution-grid cell ripple, footer fade (uses `start: 'top bottom-=40'` so it can actually fire). Fixed the timeline dots to sit centered on the dashed line (kept the plain flat 10px dot — a fancier glow/pulse version was tried and reverted as bad UX per user). Fixed the "Tutor-System" project title breaking at the hyphen (title `white-space: nowrap`, heading row `flex-wrap: wrap`). Briefly tried a pill-badge treatment for the "Building AI agent tools" link but reverted to the original underlined link per user. See ADR-013..015. Production build clean.
- 2026-07-05: Finished the in-progress JP-locale project screenshots (WebDrop, Tutor-System, TokaiHub) and committed them. Fixed "Hotel SUI Akasaka" (and long company names generally) wrapping mid-word in Work Experience — verified clean in EN and JA at all tested breakpoints (see ADR-010). Replaced the hotlinked JAL favicon with a local copy (ADR-008). Added hover tooltips (date + contribution count, localized, edge-aware positioning) to the contribution heatmap. Thickened the dashed-border style everywhere it's used. Added a `.gitignore` and untracked `node_modules`/`dist`/`.tools` (ADR-009), and deleted the redundant local `.tools/gh` binary from disk (system `gh` already installed via Homebrew). Pushed to `origin/main`.
- 2026-07-05 (same day, follow-up): Full animation/transition audit. Fixed a real jank bug where the heatmap tooltip's fade-out was invisible because `visibility` snapped to hidden before the opacity fade played (ADR-011). Added consistent transitioned hover states to every interactive element site-wide that previously had none or an abrupt one (ADR-012). Re-verified no overflow at 320/375/390/768/1280/1920px in both locales, clean production build. Pushed to `origin/main`.

- 2026-07-04: Created the agent knowledge base and scaffolded the portfolio site from an empty workspace.
- 2026-07-04: Fixed the Vite build script to run `vite build` instead of starting the dev server.
- 2026-07-04: Verified the local page in the in-app browser on desktop and mobile; all images loaded and mobile had no horizontal overflow.
- 2026-07-04: Replaced the generated avatar with the yacht photo, added a logo marquee for skills, strengthened dashed borders, resized the heatmap cells, and swapped project previews to live site screenshots.
- 2026-07-04: Matched more reference measurements: Figtree/JetBrains fonts, 700px outer and 664px inner layout, 120x124 avatar, real LinkedIn QR asset, two opposite-direction skill marquee rows, 1.5px dashed borders, reference-style button shadows, and 300x170 project screenshots.
- 2026-07-04: Added a local transparent avatar decoration layer to mimic the reference profile image treatment, adjusted avatar overflow so the QR hover card is not clipped, rebuilt, and verified no broken images or horizontal overflow at 1280px.
- 2026-07-04: Replaced fragile remote skill/contact icons with `react-icons`, slowed and hover-paused the skill carousel, added an animated/audio rocket name action, official Altius Link and Hotel SUI Akasaka logos, expandable Japanese/English work details, live MohamedFuad16 contribution data, square mobile heatmap scrolling, bottom-right project badges, and a bilingual locale toggle. Rebuilt and verified mobile in the in-app browser with no broken images or horizontal overflow.
- 2026-07-04: Removed the avatar flower decoration, changed the profile QR behavior to a two-sided flip card, moved locale control into the profile handle row with browser-language default and saved preference, added live Tokyo time plus Tokai University status, added page-wide click ring/sound effects, and swapped Hotel SUI Akasaka to the official favicon for a readable work icon. Rebuilt and verified the flip, click effect, mobile heatmap, and no broken images in the in-app browser.
